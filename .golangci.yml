version: "2"

run:
  go: "1.25.8"
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive # Modern replacement for golint
    - gocritic # Catches common micro-optimizations
